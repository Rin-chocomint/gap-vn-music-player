{
  "name": "rin-chocomint-software-0.0.0.6",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist && rimraf %LocalAppData%\\Temp\\electron-packager",
    "build": "electron-packager . Gap --platform=win32 --arch=x64 --out=dist --icon=aset/icon.ico"
  },
  "keywords": [],
  "author": "Rin chocomint",
  "license": "ISC",
  "description": "proyek dibuat hanya untuk pembelajaran...",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2",
    "rimraf": "^6.0.1"
  }
}
